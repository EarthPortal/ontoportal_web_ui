- form_options = { url: projects_path, method: :post, html: { id: "new_project", multipart: true, novalidate: true } }
- form_options = nil if @edit_mode # Skip form creation in edit mode, as it's done by the parent

- if !@edit_mode
  = form_with(**form_options) do
    .project-form-card.mb-4{"data-controller" => "project-creation"}
      .card-body
        = render partial: 'projects/form_fields'
- else 
  .project-form-card.mb-4{"data-controller" => "project-creation"}
    .card-body
      = render partial: 'projects/form_fields'

- if @errors && @errors[:exception]
  .alert.alert-danger
    = @errors[:exception]